<div class="modal hide" id="myModal<%= user.id %>" style="display: none;">
          <div class="modal-header">
            <button class="close" data-toggle="modal" href="#myModal<%= user.id %>">×</button>
            <h3>Delete Record</h3>
          </div>
          <div class="modal-body">
            <p>Are you sure?</p>
          </div>
          <div class="modal-footer">
            <%= link_to "Yes", { :controller => "users",:action=>"delete", :user_id=>user.id }, :id => 'user' , :class => "btn btn-danger"  %>
            <a data-toggle="modal" href="#myModal<%= user.id %>" class="btn secondary">No</a>
          </div>
</div>
<a class="btn" data-toggle="modal" href="#myModal<%= user.id %>" >Delete</a>