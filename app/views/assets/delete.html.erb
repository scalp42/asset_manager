<div class="modal hide fade in" id="myModal<%= asset.id %>" style="display: none;">
          <div class="modal-header">
            <button class="close" data-toggle="modal" href="#myModal<%= asset.id %>">×</button>
            <h3>Delete Record</h3>
          </div>
          <div class="modal-body">
            <p>Are you sure?</p>
          </div>
          <div class="modal-footer">
            <%= link_to "Yes", { :controller => "assets",:action=>"delete", :asset_id=>asset.id }, :id => 'asset' , :class => "btn btn-danger"  %>
            <a data-toggle="modal" href="#myModal<%= asset.id %>" class="btn secondary">No</a>
          </div>
</div>
<a class="btn" data-toggle="modal" href="#myModal<%= asset.id %>" >Delete</a>