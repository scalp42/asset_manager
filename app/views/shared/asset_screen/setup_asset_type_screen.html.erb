<div class="modal hide fade in" id="myConfigureScreenModal<%= asset_type.id %>" style="display: none;">
  <div class="modal-header">
    <button class="close" data-toggle="modal" href="#myConfigureScreenModal<%= asset_type.id %>">×</button>
    <h3>Configure Screen</h3>
  </div>
  <div class="modal-body">
   <form method="post" action="update_asset_type_screen" class="well">
     <table>
       <tr>
         <td> <%=  select("field_type", "field_type_id", Field.all.collect {|p| [ p.name, p.id ]}, {:prompt => 'Select Field Type', :multiple => true, :size => 5 }) %> </td>
       </tr>
       <tr>
         <td>

           <input type="hidden" name='asset_type_id' id='asset_type_id' value='<%= asset_type.id %>'/></td>
       </tr>
     </table>
     <div class="modal-footer">

       <%= submit_tag "Create" ,:class => 'btn btn-primary'%>
       <a data-toggle="modal" href="#myConfigureScreenModal" class="btn secondary">No</a>
     </div>
   </form>
  </div>
</div>
<a class="btn" data-toggle="modal" href="#myConfigureScreenModal<%= asset_type.id %>" >Configure Asset Screen</a>