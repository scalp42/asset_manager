<div class="modal hide" id="myCreateAssetFieldScreenModal" style="display: none;">
  <div class="modal-header">
    <button class="close" data-toggle="modal" href="#myCreateAssetFieldScreenModal">×</button>
    <h3>Add Field To Screen</h3>
  </div>
  <div class="modal-body" align="center">

    <form method="get" class="well" action="/admin_field/update_asset_type_screen" onsubmit="return $(this).validate()">
        <table>
          <tr>
          <td> <%=  select("field", "field_id", fieldsToBeAdded.collect {|p| [ p.name, p.id ] }, {:prompt => 'Select Field'},"data-validation" => "required") %> </td>

            <%= hidden_field('asset_type', 'asset_type_id',:value=>asset.id) %>
          </tr>
        </table>
        <div class="modal-footer">

          <%= submit_tag "Create" ,:class => 'btn btn-primary'%>
          <a data-toggle="modal" href="#myCreateAssetFieldScreenModal" class="btn secondary">No</a>
        </div>
    </form>
  </div>

</div>