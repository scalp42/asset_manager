<div class="modal hide" id="myModal<%= notification.id %>" style="display: none;">
  <div class="modal-header">
    <button class="close" data-toggle="modal" href="#myModal<%= notification.id %>">×</button>
    <h3>Delete Record</h3>
  </div>
  <div class="modal-body">
    <p>Are you sure?</p>
  </div>
  <div class="modal-footer">
    <%= link_to "Yes", { :controller => "notification",:action=>"delete", :notification_id=>notification.id }, :id => 'field' , :class => "btn btn-danger"  %>
    <a data-toggle="modal" href="#myModal<%= notification.id %>" class="btn secondary">No</a>
  </div>
</div>
<a class="btn" data-toggle="modal" href="#myModal<%= notification.id %>" >Delete</a>