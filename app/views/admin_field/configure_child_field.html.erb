<%= render :template => 'shared/navbar' ,:locals => {:assetsNav => false, :searchNav => false,:adminNav => true}%>

<%= render :template => 'shared/fields/configure/'+FieldType.find(@field.field_type_id).type_name.to_s.downcase.gsub(' ','_')+'_child_configure',
           :locals => { :field => @field , :parentOption => @parentOption } %>
