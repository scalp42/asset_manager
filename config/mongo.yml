defaults: &defaults
  host: 127.0.0.1
  port: 27017

development:
  <<: *defaults
  database: asset_manager

test:
  <<: *defaults
  database: asset_manager

# set these environment variables on your prod server
production:
  <<: *defaults
  database: asset_manager
